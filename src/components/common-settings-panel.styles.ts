import styled from "@emotion/styled"
import { Box } from "@mui/material"

export const StyledPanelTitle = styled(Box)`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem;
`

export const StyledPanelContent = styled.div`
  display: flex;
  flex-direction: column;
  padding: 1rem;
  gap: 1rem;
`
